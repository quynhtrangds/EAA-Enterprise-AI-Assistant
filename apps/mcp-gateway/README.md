# MCP Gateway

Backend service cho MVP Enterprise AI Assistant.

Stack:

```text
Node.js + TypeScript + Express + Zod + pg
```

API dau tien:

```http
GET /health
```

Chay local:

```powershell
npm install
npm run dev
```
