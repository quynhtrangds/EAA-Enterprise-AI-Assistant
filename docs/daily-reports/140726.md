# Báo cáo Unit Test - Ngày 14/07/2026

## 1. Trọng tâm công việc
Trong ngày hôm nay, công việc tập trung hoàn toàn vào việc xây dựng và hoàn thiện bộ **Unit Test** cho các component và tính năng cốt lõi của ứng dụng `chat-ui`.

## 2. Chi tiết Unit Test đã triển khai

### 2.1. Testing các UI Components
- **`LoginScreen.test.tsx`** (Màn hình đăng nhập):
  - Đảm bảo form đăng nhập render đúng các trường thông tin.
  - Xử lý các logic validate (kiểm tra đầu vào) trước khi submit.
  - Tương tác với AuthContext khi người dùng điền đúng/sai thông tin.

- **`ChatWindow.test.tsx`** (Cửa sổ chat):
  - Hiển thị màn hình chờ (trống) khi chưa có hội thoại.
  - Render danh sách tin nhắn (`MessageBubble`) một cách chuẩn xác.
  - Đảm bảo tính năng cuộn (auto-scroll) hoạt động khi có tin nhắn mới hoặc khi đang loading.

- **`ChatInput.test.tsx`** (Thanh nhập liệu chat):
  - Kiểm tra giao diện và luồng hoạt động của ô nhập liệu.
  - Xử lý trạng thái `disabled` của nút gửi khi hệ thống chờ phản hồi.
  - Kiểm tra các nút "Gợi ý câu hỏi" (Quick Prompts) để tự động điền text vào ô input.
  - Kiểm tra sự kiện submit form bằng phím Enter hoặc nút bấm.

- **`MessageBubble.test.tsx`** (Bong bóng tin nhắn):
  - Phân biệt giao diện giữa tin nhắn của User (người dùng) và Assistant (AI).
  - Tích hợp và hiển thị đúng định dạng Markdown.
  - Kiểm tra luồng gọi lại tin nhắn (Resend / Edit Message).

- **`ToolTrace.test.tsx`** (Lịch sử gọi công cụ - Tool trace):
  - Đảm bảo render đúng các steps mà AI đã thực thi (ví dụ: query Database, gọi API...).
  - Quản lý trạng thái mở rộng/thu gọn nội dung trace.

- **`Sidebar.test.tsx`** (Thanh điều hướng bên):
  - Khả năng render và tương tác với danh sách hội thoại.
  - Tính năng đánh dấu sao (Star), Đổi tên (Rename) và Xóa (Delete) phiên trò chuyện.
  - Gọi hàm tạo mới hội thoại (`createNewSession`).

- **`SearchModal.test.tsx`** (Cửa sổ tìm kiếm):
  - Luồng gõ từ khoá tìm kiếm, debounce (chờ gõ xong mới search).
  - Khả năng hiển thị kết quả (matched messages) và thao tác click chuyển sang hội thoại tương ứng.

- **`MainLayout.test.tsx`** (Khung giao diện chính):
  - Xác minh việc hiển thị/thu gọn toàn bộ `Sidebar`.
  - Test luồng bật/tắt cửa sổ `SearchModal`.
  - Đảm bảo layout đáp ứng (responsive) theo các kích thước màn hình.

### 2.2. Testing State & Context
- **`AuthContext.test.tsx`** (Quản lý trạng thái xác thực):
  - Kiểm tra luồng `login`: Xác minh việc lưu trữ Token, bóc tách thông tin User.
  - Kiểm tra luồng `logout`: Đảm bảo clear toàn bộ token, đưa ứng dụng về trạng thái chưa đăng nhập.

### 2.3. Testing Custom Hooks (Logic xử lý dữ liệu)
- **`useChat.test.tsx`** (Quản lý dữ liệu trò chuyện):
  - Test các hàm gọi API: `fetchSessions`, `fetchSessionDetails`, `searchSessions`.
  - Xác minh luồng tạo hội thoại mới, đổi session và gửi tin nhắn (`sendMessage`).
  - Giả lập (Mock) hàm `fetch` để kiểm tra khả năng bắt lỗi mạng (Error Handling) và cập nhật state (messages, isLoading).

## 3. Đánh giá
- Các test cases chạy hoàn toàn độc lập, tốc độ thực thi nhanh nhờ giả lập (mocking) triệt để các module bên ngoài và HTTP request.
- Các tính năng UI từ nhỏ nhất (MessageBubble, ToolTrace) đến lớn nhất (MainLayout) của `chat-ui` đều đã được kiểm tra cẩn thận bằng Unit Test.
